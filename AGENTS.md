Code instructions 
- use clean code principles 
- follow python PEP8 coding guidelines. run flake8 for ensure adherence. 
- if code use complex nested dictionary/list, prefer to use pydantic classes for clarity. 
- avoid nested function calls, giving preferences to code debugging
- avoid long functions. try to keep the functions withing 100 LOC. Break long functions into modular smaller functions.