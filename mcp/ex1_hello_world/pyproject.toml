[project]
name = "mcp-fs-demo"
version = "0.1.0"
description = "Educational MCP file-system demo"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]",
  "langchain",
  "langchain-openai",
  "langchain-mcp-adapters",
  "langgraph",
]
